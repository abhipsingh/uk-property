.social-auth-links.text-center
  = link_to (property_user_facebook_omniauth_authorize_path+ "?detail_id=#{((@detail.id) rescue "")}"), class: 'btn btn-block btn-social btn-facebook btn-flat' do
    %i.fa.fa-facebook
    Sign in using Facebook
  = link_to property_user_google_oauth2_omniauth_authorize_path+"?detail_id=#{((@detail.id) rescue "")}", class: 'btn btn-block btn-social btn-google-plus btn-flat' do
    %i.fa.fa-google-plus
    Sign in using Google+
